#include <stdio.h>
#include <cstdint>
#include "GPU.h"

using namespace std;

GPU::GPU(MEM* MEM) {
	mem = MEM;
}

GPU::~GPU() {}

sf::Uint8 * GPU::get_Texture(sf::Uint8 * pixels)
{
	//calculate each pixel on 160x144 screen



	return pixels;
}

